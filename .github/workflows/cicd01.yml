name: Github Actions test 1번
on:
  push:
    branches:
      - main
jobs:
  My-Deploy-Job:
    runs-on: ubuntu-latest
    steps:
      - name: github actions 내장 함수 사용
        run: | 
          echo $GITHUB_SHA
          echo $GITHUB_REPOSITORY
